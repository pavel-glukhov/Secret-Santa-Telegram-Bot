## Secret Santa Telegram Bot
[![en](https://img.shields.io/badge/lang-en-green.svg)](https://github.com/pavel-glukhov/Secret-Santa-Telegram-Bot/blob/main/README.md)
[![ru](https://img.shields.io/badge/lang-ru-yelow.svg)](https://github.com/pavel-glukhov/Secret-Santa-Telegram-Bot/blob/main/README.ru.md)

## Описание
Бот для организации игры «Тайный Санта» в Telegram. Позволяет участникам удобно проводить жеребьёвку,
обмениваться пожеланиями и получать результаты. Также поддерживается анонимное общение между получателем подарка и Тайным Сантой.

Поддерживается 4 языка, переведённых с использованием машинного перевода:

1. RU
2. ENG
3. KAZ
4. UK

### Текущая версия: 0.4.1

---
## История изменений

**v 0.4.2**
* Логика инициализации приложения отделена от логики запуска.
  * Добавлены независимые режимы запуска:
    - Polling — для локальной разработки, тестирования.
    - Webhook — для production-окружений с HTTPS.
    
**v 0.4.1**
* Добавлена возможность входа в комнату по URL.
* Обновлены диалоги.
* Исправлена логика приложения.

**v 0.4.0**
* Все ORM-запросы переписаны на async.
* Исправлены ошибки интерфейса.

**v 0.3.0**
* Удалён старый способ задания даты жеребьёвки.
* Добавлен интерактивный календарь и случайный выбор времени отправки результатов.
* Исправлены ошибки.

**v 0.2.2**
* Исправлены ошибки отправки сообщений.
* Оптимизированы SQL-запросы.

**v 0.2.1**
* Исправлена ошибка получения пожеланий игроков.
* Обновлены языковые словари.

**v 0.2.0**
* Приложение переписано с Aiogram 2 на Aiogram 3.
* SQL-запросы перенесены с Tortoise ORM на SQLAlchemy.
* Добавлена поддержка новых языков.
* Исправлены ошибки.

**v 0.1.0**
* Выпущена бета-версия бота.

---

Диаграмма: https://miro.com/app/board/uXjVNxWmMtE=/?share_link_id=603886678614

## Стек технологий

1. Aiogram 3
2. FastAPI
3. SQLAlchemy 2
4. Alembic
5. PostgreSQL
6. Redis

## Возможности

### 1. Создание персональных комнат

- Любой пользователь может создать комнату для неограниченного количества участников.
    - При создании необходимо указать:
        - **Название комнаты**
        - **Бюджет**
        - **Пожелания к подарку**

```
Переменная окружения USER_ROOMS_LIMIT задаёт лимит комнат, которыми может управлять пользователь.
```

```
При создании комнаты генерируется случайный уникальный номер, его длина задаётся параметром ROOM_NUMBER_LENGTH.
```

### 2. Вход в существующую комнату по ID

### 3. Управление профилем

- Бот позволяет вводить и изменять:
    - **Имя и фамилию**
    - **Домашний адрес**
    - **Номер телефона**
    - **Часовой пояс**
    - **Язык интерфейса**
    - **Полное удаление данных**

```
Адрес и номер телефона шифруются в базе данных с использованием алгоритма Fernet.
```

### 4. Управление комнатой

- Меню для обычных пользователей:
    - **Покинуть комнату**
    - **Изменить пожелания**
- Меню для владельца комнаты:
    - **Начать игру** — настройка времени распределения
        - **Настройка часового пояса**
    - **Изменить пожелания**
    - **Настройки**
        - **Удалить комнату**
        - **Изменить название комнаты**
        - **Сменить владельца**
        - **Изменить бюджет**

```
Администратор комнаты не может покинуть её, пока не передаст управление другому пользователю. Комната может быть удалена только полностью.
```

```
Если во время жеребьёвки недостаточно игроков, комнату можно активировать повторно.
```

### 5. Общение

После распределения ролей становятся доступны анонимные каналы общения:

- **Отправить сообщение Тайному Санте**
- **Отправить сообщение получателю подарка**

---

## Режимы запуска приложения

Бот поддерживает два независимых режима работы:

### 🔹 Polling (long polling)
- Предназначен для локальной разработки, тестирования и небольших ботов
- Не требует публичного HTTPS-доступа
- Использует long polling для получения обновлений от Telegram

Команда запуска:
```
python -m app.runtimes.polling
```

### 🔹 Webhook
- Предназначен для production-окружений
- Требует публичного HTTPS-домена
- Использует FastAPI как ASGI-приложение

Команда запуска:
```
uvicorn app.runtimes.webhook:create_app
```

Оба режима используют единое ядро приложения и общую бизнес-логику.

---

## Запуск бота

- Создайте собственный `.env` файл на основе `.env.example`
- Для генерации параметра **ENCRYPT_SECRET_KEY** используйте:
```
python .\manage.py generate_key
```

### Ручной запуск

- Установите PostgreSQL и Redis, настройте их и создайте базу данных
    - Для Redis необходимо включить пароль:
```
sudo nano /etc/redis/redis.conf
# requirepass foobared
```

- Установите зависимости:
```
pip install -r requirements.txt
```

- Примените миграции:
```
alembic upgrade head
```

### Запуск в режиме Polling

```
python -m app.runtimes.polling
```

### Запуск в режиме Webhook

```
uvicorn app.runtimes.webhook:create_app
```

---

## Регистрация webhook

При запуске в режиме **webhook** регистрация webhook происходит автоматически при старте приложения.

Также возможна ручная регистрация:
```
python .\manage.py register_webhook
```

Или с помощью GET-запроса:
```
https://api.telegram.org/bot{telegram_token}/setWebhook?url=https://{domain_name}/bot/
```

Пример:
```
https://api.telegram.org/bot1234567890:AAABBBCCCDDDEEEFFF0000000_FFFFF/setWebhook?url=https://example.ngrok-free.app/bot/
```

---

## Резервное копирование

Резервное копирование базы данных можно добавить в cron:
```
docker exec -t <container_name> pg_dump -U <username> <database_name> > <file_name>.sql
```

Или использовать готовый скрипт из `deploy/backup` (требуется настройка).

---

## Права доступа

Назначение прав суперпользователя:
```
python .\manage.py set_superuser <telegram_user_id>
```

Удаление прав суперпользователя:
```
python .\manage.py remove_superuser <telegram_user_id>
```

